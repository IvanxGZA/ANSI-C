#include <stdio.h>

int main()
{
  int c, nl,bl,tb;

  nl = bl = tb = 0;

  while ((c = getchar()) != EOF) {
    if (c == '\n')
      ++nl;
    if (c == '\t')
      ++tb;
    if (c == ' ')
      ++bl;
  }
  printf("New lines: %d Tabs: %d Blanks: %d\n", nl, tb, bl);
}
